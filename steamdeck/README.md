# GigDash on Steam Deck

Fallback/parallel port target. Reuses `display/` unchanged; this directory
holds SteamOS-specific kiosk plumbing (gamescope session launcher,
autostart config) analogous to `pi/`.

Status: scaffolding only — not yet implemented.
